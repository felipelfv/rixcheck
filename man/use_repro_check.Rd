% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_repro_check.R
\name{use_repro_check}
\alias{use_repro_check}
\title{Add a continuous reproducibility check to a Nix-pinned project}
\usage{
use_repro_check(
  run,
  outputs = NULL,
  tolerance = NULL,
  schedule = "0 8 1 * *",
  workflow = "repro-check.yml"
)
}
\arguments{
\item{run}{Character vector of shell commands executed inside
\code{nix-shell}, in order, e.g.
\code{c("Rscript Simulation/06_run_all.R", "quarto render Manuscript/article.qmd")}.}

\item{outputs}{Optional character vector of paths (files or directories,
as understood by \verb{git ls-files}) holding committed outputs to compare
against the freshly produced ones, e.g. \code{"Simulation/results"}. When
\code{NULL} (default), the check only verifies that \code{run} succeeds.
Point this at data outputs (\code{.rds}, \code{.csv}), not at rendered PDF/HTML:
rendered documents embed dates and are never bitwise stable.}

\item{tolerance}{Optional numeric tolerance for comparing \code{.rds} outputs
via \code{all.equal()}. When \code{NULL} (default), all comparisons are bitwise
(file hashes), which is the appropriate expectation for a pinned
environment on a fixed platform. Set a tolerance only for outputs of
genuinely nondeterministic steps (multithreaded BLAS, parallel RNG).}

\item{schedule}{Cron expression for periodically re-running the check, or
\code{NULL} to only run on push. Defaults to monthly, so the project learns
that its recipe has decayed before a reader does.}

\item{workflow}{File name of the workflow to write.}
}
\value{
Invisibly, the path to the written workflow file.
}
\description{
Writes a GitHub Actions workflow that rebuilds the project's pinned Nix
environment from its \code{default.nix}, runs the project's analysis and/or
rendering commands inside it, and (optionally) verifies that the outputs
committed to the repository are still reproduced.
}
\details{
The project must already contain a \code{default.nix}, for example one
generated by \code{rix::rix()}. This function deliberately does not create
one: rix defines the environment, rixcheck proves it still works.
}
