% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_repro_dockerfile.R
\name{use_repro_dockerfile}
\alias{use_repro_dockerfile}
\title{Add a Dockerfile that runs the project through its Nix environment}
\usage{
use_repro_dockerfile(run)
}
\arguments{
\item{run}{Single shell command executed inside \code{nix-shell} when the
container starts, e.g. \code{"quarto render Manuscript/article.qmd"}.}
}
\value{
Invisibly, the path to the written Dockerfile.
}
\description{
Writes a Dockerfile that installs Nix inside a container, builds the
project's pinned environment from its committed \code{default.nix}, and runs
the given command through \code{nix-shell}. The container is a convenience
wrapper for users without Nix; the environment itself is still defined
entirely by \code{default.nix}, so the Docker layer adds portability without
adding a second, competing environment description.
}
